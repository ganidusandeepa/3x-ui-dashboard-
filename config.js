const CONFIG = {
    // We use /api-proxy to route through our local Node server to avoid CORS issues on mobile
    PANEL_URL: window.location.origin + "/api-proxy", 
    USERNAME: "your_admin_username",
    PASSWORD: "your_admin_password"
};
